# yaml-language-server: $schema=https://raw.githubusercontent.com/agentuity/cli/refs/heads/main/agentuity.schema.json

# ------------------------------------------------
# This file is generated by Agentuity
# You should check this file into version control
# ------------------------------------------------

# The version semver range required to run this project
version: '>=0.0.103'
# The ID of the project which is automatically generated
project_id: proj_d4bf076cbb17515dab2809b7e8442d63
# The name of the project which is editable
name: Kalshi-Agentic-Agent
# The description of the project which is editable
description: Autonomous Agent for Trading Sport Event Contracts on the Kalshi Platform
# The development configuration for the project
development:
  # The port to run the development server on which can be overridden by setting the PORT environment variable
  port: 3500
  watch:
    # Whether to watch for changes and automatically restart the server
    enabled: true
    # Rules for files to watch for changes
    files:
      - agentuity_agents/**
  # The command to run the development server
  command: uv
  # The arguments to pass to the development server
  args:
    - run
    - --env-file
    - .env
    - --env-file
    - .env.development
    - server.py
deployment:
  command: uv
  args:
    - run
    - server.py
  # You should tune the resources for the deployment
  resources:
    # The memory requirements
    memory: 250Mi
    # The CPU requirements
    cpu: 500M
    # The disk size requirements
    disk: 300Mi
  # The deployment mode
  mode:
    # on-demand or provisioned
    type: on-demand
# You should not need to change these value
bundler:
  enabled: true
  identifier: python-uv
  language: python
  runtime: uv
  agents:
    dir: agentuity_agents
  ignore:
    - '**/__pycache__/**'
# The agents that are part of this project
agents:
  - # DataCoordinator Agent
    id: agent_data_coordinator_001
    name: DataCoordinator
    description: Manages all streaming data via unified StreamManager
    handler: agentuity_agents.DataCoordinator.agent
    
  - # StrategyAnalyst Agent
    id: agent_strategy_analyst_001
    name: StrategyAnalyst
    description: Analyzes market opportunities and generates trading signals
    handler: agentuity_agents.StrategyAnalyst.agent
    
  - # MarketEngineer Agent  
    id: agent_market_engineer_001
    name: MarketEngineer
    description: Analyzes sentiment and identifies market inefficiencies
    handler: agentuity_agents.MarketEngineer.agent
    
  - # TradeExecutor Agent
    id: agent_trade_executor_001
    name: TradeExecutor
    description: Executes trades on Kalshi using Kelly Criterion
    handler: agentuity_agents.TradeExecutor.agent
    
  - # RiskManager Agent
    id: agent_risk_manager_001
    name: RiskManager
    description: Manages portfolio risk and monitors positions
    handler: agentuity_agents.RiskManager.agent
