---
title: 'BacktestEngine'
description: 'High-performance backtesting engine for prediction market trading strategies'
---

## Overview

High-performance backtesting engine for prediction market trading strategies.

Features:
- Event-driven simulation
- Realistic order execution
- Portfolio tracking
- Risk management
- Performance analytics

## Constructor

<ParamField path="config" type="BacktestConfig" required>
  config parameter
</ParamField>

<CodeGroup>

```python Python
from neural_sdk.engine import BacktestEngine

# Initialize BacktestEngine
instance = BacktestEngine()
```

</CodeGroup>

## Methods

### `__init__(config)`

None

<ParamField path="config" type="BacktestConfig">
  config parameter
</ParamField>

---

### `add_strategy(strategy)`

Add trading strategy to backtest.

Args:
    strategy: Function that takes market data and returns trading signals
             Should return dict with keys: action, size, market, price (optional)

<ParamField path="strategy" type="Callable[[Dict[str, Any]], Optional[Dict[str, Any]]]">
  strategy parameter
</ParamField>

---

### `load_data(source)`

Load historical data from various sources.

Args:
    source: Data source type ('file', 's3', 'postgres', 'api')
    **kwargs: Source-specific parameters

Returns:
    DataFrame with OHLCV data

<ParamField path="source" type="str">
  source parameter
</ParamField>

<ResponseField name="result" type="pd.DataFrame">
  Method result
</ResponseField>

---

### `run()`

Execute backtest and return results.

Returns:
    BacktestResults object with portfolio performance and trades

<ResponseField name="result" type="BacktestResults">
  Method result
</ResponseField>

---

